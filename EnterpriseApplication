


Register Module(any role):
            - Register person
                        - register by role
                        -address 
                        -person name
                        -credit card (every user registered has cost)
            - Upload docs
            - validate docs.
            - azure service docs.
            
Validation Module (admin, operator role):
             - view docs
             - validate data
             - aprobe registration
             
Ticket Module (admin, operator person):
            - new register ticket
            - update service
            
Report Module:
            - ergistered People 
            -sales
            
  services:
     -data 
     -docs
     -payment
     -report
     -security      
     
     azure:
         - bus
         - trigger
         - cache memory
         -  AI doc reviwer
